
```
// 1. 定义一个整数类型的数组，放入几个整数值
int[] arr = new int[8];
arr[0] = 6;
arr[1] = 9;
// 2. 定义一个User类，封装一下属性
public class User{
    String name;
    int age;
    String sex;
}
//3. 根据上面这个类，来创建3个对象
User u1 = new User();
u1.name = "sz"

// 根据上面的User类，创建3个对象，并将这3个对象放入到一个数组中
User u1 = new User();
User u2 = new User();
User u3 = new User();

// int [] arr = new int[8]
User[] us = new User[3]
```